<?php
// config/qr.php

return [
    /*
    |--------------------------------------------------------------------------
    | Размер QR-кода (в пикселях)
    |--------------------------------------------------------------------------
    */
    'size' => env('QR_SIZE', 300),

    /*
    |--------------------------------------------------------------------------
    | Отступы вокруг QR-кода
    |--------------------------------------------------------------------------
    */
    'margin' => env('QR_MARGIN', 10),

    /*
    |--------------------------------------------------------------------------
    | Цвета QR-кода (RGB массивы)
    |--------------------------------------------------------------------------
    */
    'foreground' => [
        'red' => env('QR_FOREGROUND_RED', 0),
        'green' => env('QR_FOREGROUND_GREEN', 0),
        'blue' => env('QR_FOREGROUND_BLUE', 0),
    ],

    'background' => [
        'red' => env('QR_BACKGROUND_RED', 255),
        'green' => env('QR_BACKGROUND_GREEN', 255),
        'blue' => env('QR_BACKGROUND_BLUE', 255),
    ],

    /*
    |--------------------------------------------------------------------------
    | Уровень коррекции ошибок: Low, Medium, Quartile, High
    |--------------------------------------------------------------------------
    */
    'error_correction' => env('QR_ERROR_CORRECTION', 'High'),

    /*
    |--------------------------------------------------------------------------
    | Путь для сохранения QR-кодов
    |--------------------------------------------------------------------------
    */
    'storage_path' => env('QR_STORAGE_PATH', 'qrcodes'),

    /*
    |--------------------------------------------------------------------------
    | Настройки по умолчанию для логотипа
    |--------------------------------------------------------------------------
    */
    'logo' => [
        'width' => env('QR_LOGO_WIDTH', 80),
        'height' => env('QR_LOGO_HEIGHT', 80),
        'punchout_background' => env('QR_LOGO_PUNCHOUT', true),
    ],
];